const inquirer = require('inquirer');
const fs = require('fs');

const questions = [
    'What is your github username?',
    'What is the name or URL of the repository you wish to generate a README for?'
];

function writeToFile(fileName, data) {
    
}